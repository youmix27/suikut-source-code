<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:suikut.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="suikut.Views.LoginView"
             x:DataType="vm:LoginViewModel">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" 
                   HorizontalAlignment="Center">SE CONNECTER</TextBlock>
        <Button DockPanel.Dock="Bottom" 
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center">créer un compte</Button>
        <Button DockPanel.Dock="Bottom" 
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center">valider</Button>
        <TextBox AcceptsReturn="True"
                 Watermark="Enter your to do item"/>
    </DockPanel>
</UserControl>
